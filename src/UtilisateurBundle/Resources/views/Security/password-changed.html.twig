{% extends 'base-login.html.twig' %}

{% block title %}
    Mot de passe changé
{% endblock %}
{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('bundles/utilisateur/css/login.css') }}">
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script src="{{ asset('bundles/fosjsrouting/js/router.js') }} "></script>
    <script src="{{ path('fos_js_routing_js', {'callback': 'fos.Router.setData'}) }}"></script>
    <script src="{{ asset('bundles/utilisateur/js/first-login.js') }}"></script>
{% endblock %}

{%- block body_class -%}
    gray-bg
{%- endblock -%}
{% block body %}
    <div class="row">
        <div id="first-login-container" class="col-lg-4 col-lg-offset-4 fadeInDown">
            <div class="text-center"><i class="fa fa-check-circle fa-3x"></i></div>
            <div class="alert alert-success text-center">
                <h3>Votre mot de passe a été modifié avec succès.</h3>
                <p>Vous aller être redirigé vers la page de connexion dans un instant.</p>
                <p>Si ce n'est pas le cas, cliquer sur le bouton <strong>Connexion</strong> suivant.</p>
                <br>
                <br>
                <div>
                    <a class="btn btn-primary btn-block" href="{{ path('login') }}">Connexion</a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}